#pragma once

/// \file parser.h Parser for a busrpc-compliant directory structure.

namespace busrpc {

/// Parser, which handles busrpc directory structure and extracts busrpc entities from it.
class Parser {
public:

private:
};
} // namespace busrpc